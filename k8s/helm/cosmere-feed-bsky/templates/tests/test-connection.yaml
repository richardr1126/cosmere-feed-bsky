apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmere-feed-bsky.fullname" . }}-test-connection"
  labels:
    {{- include "cosmere-feed-bsky.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosmere-feed-bsky.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
