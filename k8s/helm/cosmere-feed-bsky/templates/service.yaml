apiVersion: v1
kind: Service
metadata:
  name: {{ include "cosmere-feed-bsky.fullname" . }}
  labels:
    {{- include "cosmere-feed-bsky.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "cosmere-feed-bsky.selectorLabels" . | nindent 4 }}
